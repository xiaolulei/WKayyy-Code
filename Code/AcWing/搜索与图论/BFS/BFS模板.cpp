#include <iostream>
#include <queue>

using namespace std;

int dfs()
{
    //定义变量
    //导入队列
    queue<int> q;
    //push一个起始元素
    while(q.size())
    {
        //弹出一个点用来做当前的第一个点，即下方for循环中间那个点
        //for(循环上下左右四个点)
        //if(检查条件是否正确)
        //如果符合条件，则向队列中push一个点用于下次一次搜索
        
    }
    // return距离，return也可以写在while循环中。
}

int main()
{
    //处理输入

    cout << dfs() << endl;
    //dfs中也可以传入参数
    return 0;
}