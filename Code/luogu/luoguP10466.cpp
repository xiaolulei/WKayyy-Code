#include <iostream>

using namespace std;

const int MAX = 1e5+10;

int num[MAX];
int snum[MAX],nt[MAX];

int main()
{
    int n,end; //end为当前检索的
    scanf("%d",&n);
    for(int i = 0 ; i < n; i ++)    scanf("%d",&num[i]);
    for(int i = 1 ; i < n; i ++)
    {
        
    }
    return 0;
}